{
  "name": "quinoa-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p build && echo $MODE > build/index.html && cp -R public/* build/",
    "build2": "mkdir -p build && echo $BUILD > build/index.html && cp -R public/* build/",
    "start": "MODE=live-coding npm run build && node server.js",
    "postinstall": "echo \"hello\" > node_modules/installed"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
