{
    "name": "quinoa-app",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build:prod": "webpack --mode=production",
        "build:dev": "webpack --mode=development",
        "build:auto": "echo $FOO; if [[ $MODE == \"dev\" ]]; then npm run build:dev; else npm run build:prod; fi",
        "test": ""
    },
    "dependencies": {
        "lit": "^2.2.1"
    },
    "browser": {
        "[module-name]": false
    },
    "devDependencies": {
        "copy-webpack-plugin": "^10.2.4",
        "webpack": "^5.70.0",
        "webpack-cli": "^4.9.2"
    }
}
